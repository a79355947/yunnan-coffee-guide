# 安全策略

## 支持范围

当前维护 `main` 分支。项目是静态 H5，加上一个可选的 Netlify 函数，用于把匿名风味准确度反馈写入飞书多维表。

## 报告漏洞

请不要在公开 Issue 中披露可利用细节、访问 token、截图中的敏感信息或真实顾客数据。

请通过 GitHub 账户 `a79355947` 的公开联系渠道报告，并提供：

- 受影响的文件、页面或函数路径；
- 可复现步骤与预期/实际行为；
- 影响范围，例如未经授权写入、跨域滥用、开放重定向或凭证泄露；
- 建议的缓解方案（如有）。

收到报告后，维护者会确认、评估影响、修复并在适当时公开说明。

## 当前安全边界

- 真实飞书凭证只能存放在 Netlify 环境变量；不得提交到仓库。
- `orderUrl` 是潜在外部跳转面；生产部署应使用 HTTPS 域名白名单。
- 反馈函数接收浏览器请求；生产部署前应配置受控 CORS、速率限制与滥用监测。
- 第三方 PR 修改 SKU、部署配置或 Netlify 函数时，需要维护者人工审查。
- 当前项目未接入 LLM、Shell、文件系统写入或任意代码执行能力；未来若增加此类能力，将重新评估提示注入、凭证与网络权限风险。
