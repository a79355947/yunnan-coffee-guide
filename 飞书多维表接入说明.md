# 一杯见云｜飞书多维表接入说明

网页已预留安全的数据采集接口：`/api/feishu-events`。飞书密钥仅保存在 Netlify 环境变量中，不会出现在手机网页或项目源码里。

## 1. 创建多维表字段

在目标多维表中新建一张数据表，并使用以下字段名称与类型：

| 字段名称 | 推荐类型 | 用途 |
| --- | --- | --- |
| 记录时间 | 文本 | 事件发生时间（ISO 格式） |
| 事件类型 | 单行文本 | 固定为“风味描述准确度” |
| 产品名称 | 单行文本 | 推荐产品名称 |
| 准确度评分 | 数字 | 用户提交的 1–5 分准确度评分 |
| 用户反馈 | 多行文本 | 用户可选反馈 |
| 会话ID | 单行文本 | 匿名体验标识，用于一次体验仅提交一次，不含手机号或姓名 |

## 2. 创建并授权飞书自建应用

1. 在飞书开发者后台创建自建应用。
2. 为应用申请并发布多维表格读写权限（`bitable:app`）。
3. 将该应用添加为目标多维表的协作者，确保它有编辑权限。
4. 从多维表链接中取得 `app_token` 与 `table_id`。常见链接形态为：`.../base/{app_token}?table={table_id}`。

## 3. 在 Netlify 后台设置环境变量

进入 Netlify 项目的 **Environment variables**，新增以下四项：

```text
FEISHU_APP_ID=你的飞书应用 App ID
FEISHU_APP_SECRET=你的飞书应用 App Secret
FEISHU_BITABLE_APP_TOKEN=多维表 app_token
FEISHU_BITABLE_TABLE_ID=数据表 table_id
```

设置后重新部署站点。不要把 App Secret 写入网页、Markdown 或聊天消息。

## 4. 写入的数据

页面会在浏览器本地记录完成风味匹配、更新推荐、加入购物车、点击下单、保存风味卡、打开核验、提交评分等动作。为避免无关行为数据外发，只有用户主动提交的“风味描述准确度”评分会在飞书配置完成后同步写入多维表；未配置时页面仍可正常使用，本地记录不受影响。
